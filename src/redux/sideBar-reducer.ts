let initialState = {};

const sideBarReducer = (
  state = initialState,
  action: any
): initialStateType => {
  return state;
};

export default sideBarReducer;

type initialStateType = typeof initialState;
